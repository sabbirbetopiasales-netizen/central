rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Sales leaderboard public displays and synchronization
    match /leaderboard_data/{document} {
      allow read, write: if true;
    }
  }
}
